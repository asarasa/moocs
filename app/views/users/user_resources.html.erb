<% if @resources.empty? %>
	<h3 style="text-align:center">No has creado ningún recurso.</h3>
<% else %>
	<div class="page-header">
		<h2>Listing my resources</h2>
	</div>

	<table class="table table-stripped">
	  <thead>
	    <tr>
	      <th>Name</th>
	      <th>Type</th>
	      <th>Url</th>
	      <th></th>
	      <th></th>
	      <th></th>
	    </tr>
	  </thead>

	  <tbody>
	    <% @resources.each do |resource| %>
	      <tr>
	        <td><%= resource.name %></td>
	        <td>
		      	<% if resource.type == "video" %>
		      		<%= image_tag("1387049800_social_4.png", :alt => "video") %>
		      	<% elsif resource.type == "audio" %>
		      		<%= image_tag("1387133987_media-optical-audio.png", :alt => "audio") %>
		      	<% elsif resource.type == "pdf" %>
		      		<%= image_tag("1387134021_application-pdf.png", :alt => "PDF") %>
		      	<% elsif resource.type == "text" %>
		      		<%= image_tag("1387134064_readme.png", :alt => "Text") %>
		      	<% end %>
	        </td>
	        <td><%= resource.url %></td>
	        <td><%= link_to 'Show', resource %></td>
	        <td><%= link_to 'Edit', edit_resource_path(resource) %></td>
          <td><%= link_to 'Add Quiz', resource_quizzes_path(resource)%></td>
	        <td><%= link_to 'Destroy', resource, method: :delete, data: { confirm: 'Are you sure?' }%></td>
         
      </tr>
	    <% end %>
	  </tbody>
	</table>
<% end %>
<center>
	<%= link_to 'New resource', new_resource_path, :class => 'btn btn-success' %>
</center>