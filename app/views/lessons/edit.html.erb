<h3>Course: <%= @course.name %></h3>
<div class="form-signin">
	<h2 class="form-signin-heading">Editing lesson</h2>

<%= render 'form' %>

</div>

<%= link_to 'Show', course_lesson_path(@course, @lesson), :class => 'btn btn-info' %>
<%= link_to 'Back', course_lessons_path(@course), :class => 'btn btn-default' %>